{
  "name": "fringe-frontend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev src",
    "deploy": "wrangler pages deploy src",
    "preview": "wrangler pages dev src --port 3000"
  }
}